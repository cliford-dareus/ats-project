import React from 'react';
import {ApplicationResponseType} from "@/types/job-listings-types";

const CandidateList = ({application}:{application: ApplicationResponseType[]}) => {
    return (
        <div>
            
        </div>
    );
};

export default CandidateList;