import React from 'react';

type Props = {
    data: any;
}

const CandidateResume = ({data}: Props) => {
    return (
        <div>
            Candidate Resume
        </div>
    );
};

export default CandidateResume;