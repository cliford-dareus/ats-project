import React from 'react';

// type Props = {}

const Page = async () => {
    // const {userId, redirectToSignIn} = await auth();
    return (
        <div className="">ff</div>
    )
};

export default Page;